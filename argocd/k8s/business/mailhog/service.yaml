apiVersion: v1
kind: Service
metadata:
  name: mailhog-svc
  namespace: ruralxperience
spec:
  selector:
    app: mailhog
  ports:
    - name: smtp
      port: 1025
      targetPort: 1025
    - name: web-ui
      port: 8025
      targetPort: 8025